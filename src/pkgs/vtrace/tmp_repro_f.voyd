use std::random::{ LocalRng, Random }

eff RenderCtx
  rand_f64(tail) -> f64

pub fn main(): Random -> f64
  let ~rng = LocalRng::seeded_from_host()
  try forward
    let a = RenderCtx::rand_f64()
    let b = RenderCtx::rand_f64()
    a + b
  RenderCtx::rand_f64(tail):
    tail(rng.random_f64())
